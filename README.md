# Алгоритм поиска стеллажа по данным лидара

> 📖 **Демонстрация работы алгоритма**: см. [DEMO.md](./DEMO.md) для примеров использования и визуализации результатов

## Запуск проекта

### 1. Получение и подготовка проекта

```bash
git pull
cd dikom
```

### 2. Запуск Docker контейнера

```bash
docker-compose up
```

Это запустит ROS2-симуляцию с нодой поиска стеллажа.

### 3. Вызов сервиса из другого терминала

Для вызова сервиса поиска стеллажа из другого терминала выполните:

**Шаг 1**: Подключитесь к запущенному контейнеру:
```bash
docker exec -it $(docker ps --filter "name=dikom-ros2" --format "{{.ID}}") bash -lc "source /opt/ros/humble/setup.bash && source /workspace/install/setup.bash && exec bash"
```

**Шаг 2**: Вызовите сервис поиска стеллажа:
```bash
ros2 service call /find_rack amr/srv/FindRack "{leg_width: 0.1, leg_spacing: 1, entry_depth: 0.50}"
```

Параметры запроса:
- `leg_width`: ширина ножки стеллажа в метрах (например, 0.1 = 10 см)
- `leg_spacing`: расстояние между ножками в метрах (например, 1.0 = 1 метр)
- `entry_depth`: глубина заезда в стеллаж в метрах (например, 0.50 = 50 см)

## Описание работы алгоритма

Алгоритм предназначен для обнаружения стеллажа по данным лидара и вычисления целевой позы для навигации робота. Работает в виде ROS2-ноды, которая предоставляет сервис `find_rack`.

### Основные этапы работы:

1. **Получение данных лидара**
   - Нода подписывается на топик `LaserScan` и сохраняет последнее полученное сообщение
   - Данные используются при вызове сервиса поиска стеллажа

2. **Нормализация данных**
   - Заменяет некорректные значения дальности (None, бесконечные, отрицательные) на `range_max`
   - Обеспечивает корректную обработку данных лидара

3. **Вычисление разностей между лучами**
   - Вычисляет разность дальности между соседними лучами лидара
   - Позволяет обнаружить резкие изменения дальности, характерные для ножек стеллажа

4. **Обнаружение краёв ножек**
   - Ищет скачки дальности, превышающие порог `diff_threshold`
   - Вход на ножку: резкое уменьшение дальности (diff < -threshold)
   - Выход с ножки: резкое увеличение дальности (diff > threshold)
   - Ограничивает максимальную ширину ножки параметром `max_width_rays`

5. **Вычисление центров ножек**
   - Для каждой найденной пары краёв вычисляет центр ножки в декартовых координатах (x, y)
   - Использует полярные координаты лидара (дальность и угол)

6. **Фильтрация кандидатов**
   - Отфильтровывает ножки, ширина которых превышает `leg_width * 2.0`
   - Сортирует кандидатов по расстоянию от робота (ближайшие первыми)
   - Ограничивает количество обрабатываемых ножек параметром `max_legs`

7. **Поиск пары ножек стеллажа**
   - Перебирает все возможные пары найденных ножек
   - Проверяет соответствие параметрам:
     - Ширина каждой ножки должна соответствовать `leg_width` (допуск: 50% от ширины)
     - Расстояние между ножками должно соответствовать `leg_spacing` (допуск: 30% от расстояния)
   - Выбирает лучшую пару:
     - При 2 ножках: проверяет соответствие параметрам
     - При 3 ножках: выбирает пару с минимальной суммарной ошибкой
     - При 4+ ножках: выбирает ближайшую к роботу пару среди подходящих

8. **Вычисление целевой позы**
   - Вычисляет центр между двумя ножками
   - Определяет направление перпендикулярно линии между ножками, направленное от робота
   - Смещает целевую позицию на `entry_depth` в направлении к стеллажу
   - Вычисляет ориентацию (yaw) на основе направления движения

9. **Отправка цели навигации**
   - Публикует целевую позу в топик `/goal_pose`
   - Возвращает результат через сервис `FindRack`

## Настраиваемые параметры

### Параметры ноды (ROS2 параметры)

#### `scan_topic` (по умолчанию: `"scan"`)
- **Тип**: строка
- **Описание**: Название топика, на который подписывается нода для получения данных `LaserScan`
- **Влияние**: Определяет источник данных лидара. Если ваш лидар публикует данные в другой топик, укажите его здесь

#### `diff_threshold` (по умолчанию: `0.15`)
- **Тип**: float (метры)
- **Описание**: Порог для обнаружения скачков дальности при детекции ножек
- **Влияние**: 
  - **Меньшие значения** (0.05-0.10): более чувствительное обнаружение, может находить больше ложных срабатываний от шумов
  - **Большие значения** (0.20-0.30): менее чувствительное обнаружение, пропускает тонкие ножки, но более устойчиво к шумам
  - **Рекомендуется**: 0.10-0.20 для типичных условий

#### `max_width_rays` (по умолчанию: `30`)
- **Тип**: целое число
- **Описание**: Максимальная ширина ножки в количестве лучей лидара
- **Влияние**:
  - **Меньшие значения** (10-20): обнаруживает только тонкие ножки, пропускает широкие объекты
  - **Большие значения** (40-60): может обнаруживать более широкие ножки, но может захватывать части других объектов
  - **Рекомендуется**: 20-40 в зависимости от углового разрешения лидара и размера ножек

#### `max_legs` (по умолчанию: `8`)
- **Тип**: целое число
- **Описание**: Максимальное количество ножек для обработки (0 = без ограничений)
- **Влияние**:
  - **Меньшие значения** (2-4): обрабатывает только ближайшие ножки, быстрее работает
  - **Большие значения** (8-16): может найти стеллаж среди множества объектов, но медленнее
  - **0**: обрабатывает все найденные ножки (может быть медленно при большом количестве объектов)
  - **Рекомендуется**: 4-8 для типичных условий

#### `range_max` (по умолчанию: `0.0`)
- **Тип**: float (метры)
- **Описание**: Максимальная дальность лидара для нормализации (0.0 = использовать значение из LaserScan)
- **Влияние**:
  - **0.0**: использует `scan.range_max` из сообщения лидара (рекомендуется)
  - **Положительное значение**: переопределяет максимальную дальность, используется для нормализации некорректных значений
  - **Рекомендуется**: оставить 0.0, если лидар корректно передаёт `range_max`

### Параметры запроса сервиса `FindRack`

#### `leg_width` (обязательный)
- **Тип**: float (метры)
- **Описание**: Ожидаемая ширина ножки стеллажа
- **Влияние**:
  - Используется для фильтрации кандидатов (отбрасываются ножки шире `leg_width * 2.0`)
  - Используется для проверки соответствия пары ножек (допуск: ±50% от `leg_width`)
  - **Важно**: Должен соответствовать реальной ширине ножек вашего стеллажа
  - **Пример**: 0.05 м (5 см) для тонких ножек, 0.10 м (10 см) для широких

#### `leg_spacing` (обязательный)
- **Тип**: float (метры)
- **Описание**: Ожидаемое расстояние между двумя ножками стеллажа
- **Влияние**:
  - Используется для поиска пары ножек, соответствующей параметрам стеллажа
  - Допуск: ±30% от `leg_spacing`
  - **Важно**: Должно соответствовать реальному расстоянию между ножками вашего стеллажа
  - **Пример**: 0.60 м (60 см) для типичного стеллажа

#### `entry_depth` (обязательный)
- **Тип**: float (метры)
- **Описание**: Глубина, на которую робот должен заехать в стеллаж от центра между ножками
- **Влияние**:
  - Определяет финальную позицию робота относительно центра между ножками
  - Положительное значение: робот заезжает внутрь стеллажа
  - Отрицательное значение: робот остаётся перед стеллажом
  - **Рекомендуется**: 0.2-0.5 м в зависимости от размеров робота и стеллажа

